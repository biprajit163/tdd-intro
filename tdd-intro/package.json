{
  "name": "tdd-intro",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "jest --watchALL --verbose"
  },
  "devDependencies": {
    "vite": "^2.7.0"
  },
  "dependencies": {
    "jest": "^27.4.3"
  }
}
